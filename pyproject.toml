[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "live_tracking"
authors = [{name = "Alberto", email = "alberto.doimo@uni-konstanz.de"}]
dynamic = ["version", "description"]
